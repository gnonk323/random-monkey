export type UnsplashPhoto = {
  urls: {
    raw: string;
    full: string;
    regular: string;
    small: string;
    thumb: string;
  };
};

export type MonkeyImageType = {
  url: string
  favorite: boolean
}